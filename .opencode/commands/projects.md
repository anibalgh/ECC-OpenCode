---
description: List registered projects and instinct counts
agent: build
---

# Projects Command

Show continuous-learning-v2 project registry and stats: $ARGUMENTS

## Your Task

Run:

```bash
python3 "${OPENCODE_PLUGIN_ROOT}/skills/continuous-learning-v2/scripts/instinct-cli.py" projects
```

If `OPENCODE_PLUGIN_ROOT` is unavailable, use:

```bash
python3 ~/.opencode/skills/continuous-learning-v2/scripts/instinct-cli.py projects
```

